import { ElementType } from "react"

export interface IconArgs {
    icon:ElementType
    iconText:string,
    title:string,
    titleSize:string,
    iconTextSize:string,
    iconColor:string,
    titleColor:string
}